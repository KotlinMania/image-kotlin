# Port targets — `image-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `image` that codex-rs uses, and therefore the minimum subset that `image-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-66] (rank `62`, tier `direct`, status `To Do`)

**Cargo package name**: `image`
**Rust module name (in `use` statements)**: `image`

## Summary

- **15** distinct symbols imported by codex-rs
- **29** total `use` statements
- **7** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `image::ImageBuffer`

- **5 `use` site(s)** across **3 codex member crate(s)**: `codex-core`, `codex-tui`, `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/context_manager/history_tests.rs:26` (`codex-core`) — `use image::ImageBuffer`
- `core/tests/suite/image_rollout.rs:23` (`codex-core`) — `use image::ImageBuffer`
- `core/tests/suite/view_image.rs:37` (`codex-core`) — `use image::ImageBuffer`
- `tui/src/bottom_pane/chat_composer.rs:4612` (`codex-tui`) — `use image::ImageBuffer`
- `utils/image/src/lib.rs:203` (`codex-utils-image`) — `use image::ImageBuffer`

### `image::Rgba`

- **5 `use` site(s)** across **3 codex member crate(s)**: `codex-core`, `codex-tui`, `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/context_manager/history_tests.rs:29` (`codex-core`) — `use image::Rgba`
- `core/tests/suite/image_rollout.rs:24` (`codex-core`) — `use image::Rgba`
- `core/tests/suite/view_image.rs:38` (`codex-core`) — `use image::Rgba`
- `tui/src/bottom_pane/chat_composer.rs:4613` (`codex-tui`) — `use image::Rgba`
- `utils/image/src/lib.rs:204` (`codex-utils-image`) — `use image::Rgba`

### `image::DynamicImage`

- **3 `use` site(s)** across **3 codex member crate(s)**: `codex-core`, `codex-tui`, `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/suite/view_image.rs:35` (`codex-core`) — `use image::DynamicImage`
- `tui/src/history_cell.rs:79` (`codex-tui`) — `use image::DynamicImage`
- `utils/image/src/lib.rs:10` (`codex-utils-image`) — `use image::DynamicImage`

### `image::GenericImageView`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/suite/view_image.rs:36` (`codex-core`) — `use image::GenericImageView`
- `utils/image/src/lib.rs:11` (`codex-utils-image`) — `use image::GenericImageView`
- `utils/image/src/lib.rs:202` (`codex-utils-image`) — `use image::GenericImageView`

### `image::ImageFormat`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-core`, `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/context_manager/history_tests.rs:27` (`codex-core`) — `use image::ImageFormat`
- `utils/image/src/error.rs:2` (`codex-utils-image`) — `use image::ImageFormat`
- `utils/image/src/lib.rs:13` (`codex-utils-image`) — `use image::ImageFormat`

### `image::load_from_memory`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/suite/view_image.rs:39` (`codex-core`) — `use image::load_from_memory`

### `image::Luma`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/context_manager/history_tests.rs:28` (`codex-core`) — `use image::Luma`

### `image::ImageReader`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/history_cell.rs:80` (`codex-tui`) — `use image::ImageReader`

### `image::ImageError`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/image/src/error.rs:1` (`codex-utils-image`) — `use image::ImageError`

### `image::ColorType`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/image/src/lib.rs:9` (`codex-utils-image`) — `use image::ColorType`

### `image::ImageEncoder`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/image/src/lib.rs:12` (`codex-utils-image`) — `use image::ImageEncoder`

### `image::codecs::jpeg::JpegEncoder`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/image/src/lib.rs:14` (`codex-utils-image`) — `use image::codecs::jpeg::JpegEncoder`

### `image::codecs::png::PngEncoder`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/image/src/lib.rs:15` (`codex-utils-image`) — `use image::codecs::png::PngEncoder`

### `image::codecs::webp::WebPEncoder`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/image/src/lib.rs:16` (`codex-utils-image`) — `use image::codecs::webp::WebPEncoder`

### `image::imageops::FilterType`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-image`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/image/src/lib.rs:17` (`codex-utils-image`) — `use image::imageops::FilterType`
